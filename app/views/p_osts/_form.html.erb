<%= form_with(model: p_ost) do |form| %>
  <% if p_ost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(p_ost.errors.count, "error") %> prohibited this p_ost from being saved:</h2>

      <ul>
        <% p_ost.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
